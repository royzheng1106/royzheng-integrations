/**
 * Maps Telegram userId / chatId / botId -> agentId
 */
export const USER_WHITELIST: Record<string, string> = {
  "296810724": "assistant",       // Telegram userId
  // Add more mappings as needed
};
